# Android

This directory will host the Kotlin-based Android implementation for Kirie.

Planned responsibility:

- create and manage the Android WebView
- expose a Godot Android plugin singleton
- bridge messages between Godot and web content

The build output is expected to be consumed by the Godot export plugin under
`addon/addons/kirie/`.

